# Copy this file and rename it to ".env" 
# Replace the values below with your actual database credentials

# PostgreSQL Database URL
# Format: postgresql://username:password@host:port/database_name
DATABASE_URL=postgresql://postgres:your_password_here@localhost:5432/clinic_db

# Session Secret (change this to a random string for security)
SESSION_SECRET=clinic_secret_key_change_this_in_production

# Server Port (default: 3000)
PORT=3000

# Optional: Set to 'development' to see detailed logs
# NODE_ENV=development 